<UserControl x:Class="MyUIGenerator.View.frmEntityDataSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
           FlowDirection="RightToLeft"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="lblEntitySelector" Text="موجودیت"></TextBlock>
        <Grid x:Name="grdEntitySelector" Grid.Column="1"></Grid>
        <TextBlock x:Name="lblDataSelector" Text="داده" Grid.Row="1"></TextBlock>
        <Grid x:Name="grdDataSelector" Grid.Row="1" Grid.Column="1"></Grid>
        <TextBlock x:Name="lblSearchRepository" Text="جستجو" Grid.Row="2"></TextBlock>
        <Grid x:Name="grdSearchRepository" Grid.Row="2" Grid.Column="1"></Grid>

    </Grid>

</UserControl>
