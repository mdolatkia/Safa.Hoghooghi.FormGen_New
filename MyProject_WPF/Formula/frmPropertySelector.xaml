<UserControl x:Class="MyProject_WPF.frmPropertySelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            
             mc:Ignorable="d"  xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             d:DesignHeight="400"  d:DesignWidth="300" FlowDirection="RightToLeft">
    <Grid>
        <telerik:RadGridView x:Name="dtgFormulaItems" AutoGenerateColumns="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Header="نوع خصوصیت" DataMemberBinding="{Binding PropertyType}" />
                <telerik:GridViewDataColumn Header="نام" DataMemberBinding="{Binding Name}" />
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
